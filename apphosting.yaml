# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  maxInstances: 1
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# env:

secrets:
  - id: OPENAI-API-KEY
    version: 1